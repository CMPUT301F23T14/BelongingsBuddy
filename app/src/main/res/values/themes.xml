<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BelongingsBuddy" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BelongingsBuddy" parent="Base.Theme.BelongingsBuddy" />
    <style name="Button.Gray" parent="ThemeOverlay.AppCompat">
        <item name="backgroundTint">@color/login_button</item>
        <item name="fontFamily">sans-serif</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">20sp</item>
    </style>
    <style name="EditText.Gray" parent="ThemeOverlay.AppCompat">
        <item name="background">@color/login_button</item>
        <item name="fontFamily">sans-serif</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textAlignment">viewStart</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:visibility">gone</item>
        <item name="android:selectAllOnFocus">true</item>
        <item name="android:paddingLeft">8sp</item>
    </style>
</resources>